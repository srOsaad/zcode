#include <bits/stdc++.h>
using namespace std;

#define code int main()
#define boost ios::sync_with_stdio(0); cin.tie(0)
#define For(i,n) for(int i = 0; i < n;i++)
#define For_(i,x,n) for(int i=x;i<n;i++)
#define sz(x) x.size()
#define wr cout<<
#define clearTerminal system("pause && cls")
#define DBG() cout<<"ok\n";

bool systemStr(string command) {return system(command.c_str());}